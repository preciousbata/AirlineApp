import 'package:flutter/material.dart';

class SeatTab extends StatelessWidget {
  const SeatTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.only(top: 8.0),
      child: Container(
        color: Colors.blue,
      ),
    );
  }
}
